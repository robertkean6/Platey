import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Platey - UK Number Plate Supplier & Printer | Coming Soon',
  description: 'Platey is a UK-based number plate supplier and printer. We\'re launching soon with premium quality number plates for your vehicle.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}

